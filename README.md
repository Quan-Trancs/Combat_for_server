# Combat_for_server

Early Java practice project: simple combat-related **command handling** (`Combat_commands.java`) and entrypoint (`Main.java`).

Learning sketch for server-style command processing — not a full game server.
